import { FeaturePlaceholderPage } from '@/components/ui/FeaturePlaceholderPage';

export default function SurveyStacksPage() {
  return (
    <FeaturePlaceholderPage
      title="Survey stacks"
      description="Combine surveys into stacks for dashboard and report data sources"
    />
  );
}
